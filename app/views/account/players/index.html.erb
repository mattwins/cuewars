<h1>Listing players</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Nickname</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @players.each do |player| %>
  <tr>
    <td><%= player.name %></td>
    <td><%= player.nickname %></td>
    <td><%= player.email %></td>
    <td><%= link_to 'Show', [:account, player] %></td>
    <td><%= link_to 'Edit', edit_account_player_path(player) %></td>
    <td><%= link_to 'Destroy', [:account, player], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Player', new_account_player_path %>
